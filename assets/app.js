
const $=(s,scope=document)=>scope.querySelector(s); const $$=(s,scope=document)=>[...scope.querySelectorAll(s)];
function setupNav(){const menu=$('.menu');const links=$('.nav-links');if(menu)menu.addEventListener('click',()=>links.classList.toggle('open'));}
function toast(msg){let t=$('.toast');if(!t){t=document.createElement('div');t.className='toast';Object.assign(t.style,{position:'fixed',right:'20px',bottom:'20px',zIndex:9999,background:'#182038',color:'#fff',padding:'13px 16px',borderRadius:'10px',boxShadow:'0 10px 30px rgba(0,0,0,.18)'});document.body.appendChild(t)}t.textContent=msg;clearTimeout(window.__toast);window.__toast=setTimeout(()=>t.remove(),2500)}
function wirePlaceholderActions(){ $$('[data-placeholder]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();toast(b.dataset.placeholder||'Backend integration placeholder');})); }
function initCatalog(){const search=$('#catalogSearch'); const cards=$$('.catalog-card'); const buttons=$$('.filter'); let current=new URLSearchParams(location.search).get('cat')||'All'; buttons.forEach(b=>{if(b.dataset.filter===current){buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active')}}); const apply=()=>{const q=(search?.value||'').toLowerCase();cards.forEach(c=>{const okCat=current==='All'||c.dataset.category===current; const okText=c.textContent.toLowerCase().includes(q); c.style.display=okCat&&okText?'block':'none'});}; buttons.forEach(b=>b.addEventListener('click',()=>{buttons.forEach(x=>x.classList.remove('active'));b.classList.add('active');current=b.dataset.filter;apply()})); search?.addEventListener('input',apply); }
window.addEventListener('DOMContentLoaded',()=>{setupNav();wirePlaceholderActions();initCatalog();});
